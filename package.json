{
  "name": "landa-typescriptor",
  "scripts": {
    "build": "tsc",
    "deploy": "npm run build && sls deploy"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.71",
    "@types/node": "^14.14.22",
    "typescript": "^4.1.3"
  }
}
